// import { ethers } from "ethers";
// import LockABI from "../../../deployments/sepolia.json"; 

// export function getContract(signerOrProvider: ethers.Signer | ethers.providers.Provider) {
//   const contractAddress = LockABI.contract.address;
//   const abi = LockABI.contract.abi;

//   return new ethers.Contract(contractAddress, abi, signerOrProvider);
// }
